import { test, expect } from '@playwright/test';

// Placeholder smoke test to validate tooling wiring
// Real tests can navigate against `npm run preview` + baseURL

test('tooling is wired', async () => {
  expect(true).toBe(true);
});
