export type MenuItem = {
  title: string;
  price: string | number;
  description?: string;
  unit?: string;
  notes?: string;
  category?: string;
  superCategory?: string;
  quantity?: string | number;
};
