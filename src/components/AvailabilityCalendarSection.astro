---
import calendarScript from '../lib/calendar.ts?url';
---
<section id="verfuegbarkeit" class="section">
  <div class="container">
    <div class="section-header">
      <p class="eyebrow">Events &amp; Auftritte</p>
      <h2>Erlebe Archie live und in Farbe</h2>
      <p class="subtitle">
        Schlanke Navigation, klares Raster und gut lesbare Karten – optimiert für Smartphones und große Displays.
      </p>
    </div>
    <div class="card calendar-card">
      <div class="calendar-card__backdrop"></div>
      <header class="calendar-toolbar">
        <div class="calendar-nav">
          <button id="calendar-prev" class="btn btn-secondary" aria-label="Vorheriger Monat">❮</button>
          <div class="calendar-month">
            <span class="month-label">Kalender</span>
            <strong id="calendar-month"></strong>
          </div>
          <button id="calendar-next" class="btn btn-secondary" aria-label="Nächster Monat">❯</button>
        </div>
        <div class="calendar-legend" aria-label="Legende der angezeigten Termine">
          <span class="legend-dot legend-dot--primary"></span>
          <span>Bestätigte Termine</span>
        </div>
      </header>
      <div id="calendar-status" class="price-pill calendar-status" aria-live="polite">Kalender wird geladen …</div>
      <div class="calendar-layout">
        <div class="calendar-board">
          <p class="mobile-helper">Tippe auf einen Tag, um die Termine hervorzuheben.</p>
          <div id="calendar-weekdays" class="calendar__weekdays" aria-hidden="true">
            {['Mo', 'Di', 'Mi', 'Do', 'Fr', 'Sa', 'So'].map((d) => (
              <div class="calendar__weekday">{d}</div>
            ))}
          </div>
          <div id="calendar-grid" class="grid"></div>
        </div>
      </div>
    </div>
    <div class="card month-events">
      <header class="month-events__header">
        <div>
          <p class="eyebrow">Monatsübersicht</p>
          <h3>Alle Termine dieses Monats</h3>
          <p class="subtitle">Immer vollständig sichtbar – perfekt für schnelles Scrollen am Smartphone.</p>
        </div>
        <div class="pill-muted">Automatisch mit dem gewählten Monat synchronisiert</div>
      </header>
      <div id="calendar-month-events" class="month-events__list"></div>
    </div>
  </div>
</section>
<style>
  #verfuegbarkeit h2 {
    color: var(--color-corporate-white);
  }

  #verfuegbarkeit .section-header {
    display: grid;
    gap: var(--space-1);
    margin-bottom: var(--space-4);
    max-width: 760px;
  }

  #verfuegbarkeit .subtitle {
    color: var(--color-muted);
    margin: 0;
  }

  #verfuegbarkeit .eyebrow {
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-size: var(--font-size-xs);
    color: var(--color-muted);
    margin: 0;
  }

  #verfuegbarkeit .calendar-card {
    position: relative;
    overflow: hidden;
    background: linear-gradient(150deg, rgba(255, 255, 255, 0.98), rgba(255, 255, 255, 0.92)),
      radial-gradient(circle at 20% 20%, rgba(255, 113, 91, 0.08), transparent 35%),
      radial-gradient(circle at 80% 10%, rgba(74, 200, 255, 0.12), transparent 38%);
    border: 1px solid rgba(255, 255, 255, 0.2);
    box-shadow: 0 20px 60px rgba(15, 17, 21, 0.18);
    backdrop-filter: blur(6px);
  }

  #verfuegbarkeit .calendar-card__backdrop {
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at 50% -10%, rgba(255, 179, 71, 0.16), transparent 42%),
      linear-gradient(120deg, rgba(15, 17, 21, 0.04), rgba(15, 17, 21, 0));
    pointer-events: none;
    z-index: 0;
  }

  #verfuegbarkeit .calendar-card > * {
    position: relative;
    z-index: 1;
  }

  #verfuegbarkeit .calendar-toolbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: var(--space-2);
    flex-wrap: wrap;
    padding: var(--space-3) var(--space-3) var(--space-2);
    border-bottom: 1px solid rgba(15, 17, 21, 0.06);
  }

  #verfuegbarkeit .calendar-nav {
    display: flex;
    gap: var(--space-3);
    align-items: center;
  }

  #verfuegbarkeit .calendar-month {
    display: grid;
    gap: 2px;
  }

  #verfuegbarkeit .month-label {
    font-size: var(--font-size-xs);
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--color-muted);
  }

  #verfuegbarkeit .btn-secondary {
    background: #f2f2f4;
    color: #0f1115;
    border: 1px solid rgba(15, 17, 21, 0.14);
    box-shadow: 0 14px 30px rgba(15, 17, 21, 0.12);
    padding: var(--space-2);
    border-radius: 12px;
    width: 3rem;
    height: 3rem;
    font-size: 1.35rem;
    font-weight: 700;
  }

  #verfuegbarkeit .btn-secondary:hover,
  #verfuegbarkeit .btn-secondary:focus-visible {
    background: #e7e8eb;
    box-shadow: 0 16px 36px rgba(15, 17, 21, 0.18);
  }

  #verfuegbarkeit #calendar-month {
    font-size: 1.25rem;
    color: #0f1115;
    text-transform: capitalize;
  }

  #verfuegbarkeit .calendar-status {
    margin: 0 var(--space-2) var(--space-2);
  }

  #verfuegbarkeit .calendar-legend {
    display: inline-flex;
    align-items: center;
    gap: var(--space-1);
    font-size: var(--font-size-sm);
    color: var(--color-muted);
    padding: 10px 14px;
    background: rgba(15, 17, 21, 0.04);
    border-radius: 12px;
  }

  #verfuegbarkeit .legend-dot {
    width: 10px;
    height: 10px;
    border-radius: 999px;
    display: inline-block;
  }

  #verfuegbarkeit .legend-dot--primary {
    background: linear-gradient(135deg, #ff715b, #ffb347);
    box-shadow: 0 0 0 4px rgba(255, 113, 91, 0.18);
  }

  #verfuegbarkeit .calendar__weekdays {
    background: rgba(255, 113, 91, 0.08);
    padding: var(--space-2);
    border-radius: var(--radius-md);
    margin-bottom: var(--space-2);
  }

  #verfuegbarkeit .calendar-layout {
    width: 100%;
    display: grid;
    gap: var(--space-3);
    grid-template-columns: 1fr;
    align-items: stretch;
    padding: 0 var(--space-2) var(--space-3);
    box-sizing: border-box;
  }

  #verfuegbarkeit .calendar-board {
    width: 100%;
    overflow-x: auto;
    padding-bottom: var(--space-1);
  }

  #verfuegbarkeit #calendar-weekdays,
  #verfuegbarkeit #calendar-grid {
    display: grid;
    grid-template-columns: repeat(7, minmax(2.85rem, 1fr));
    gap: var(--space-1);
    width: 100%;
    min-width: 360px;
  }

  #verfuegbarkeit #calendar-weekdays {
    margin: var(--space-3) 0 var(--space-2);
  }

  #verfuegbarkeit .calendar__weekday {
    text-align: center;
    font-weight: 600;
    color: #656b77;
    letter-spacing: 0.02em;
  }

  #verfuegbarkeit .mobile-helper {
    display: none;
    color: var(--color-muted);
    font-size: var(--font-size-sm);
    padding: 0 var(--space-1);
  }

  @media (max-width: 720px) {
    #verfuegbarkeit #calendar-weekdays,
    #verfuegbarkeit #calendar-grid {
      grid-template-columns: repeat(7, minmax(2.35rem, 1fr));
      gap: 10px;
      min-width: 340px;
    }

    #verfuegbarkeit .calendar-layout {
      grid-template-columns: 1fr;
      gap: var(--space-2);
    }

    #verfuegbarkeit .calendar-toolbar {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--space-2);
    }

    #verfuegbarkeit #calendar-weekdays {
      padding: var(--space-2) var(--space-1);
      margin: var(--space-2) 0;
    }

    #verfuegbarkeit .calendar__weekday {
      font-size: var(--font-size-sm);
    }

    #verfuegbarkeit .calendar-board {
      background: rgba(15, 17, 21, 0.02);
      border-radius: var(--radius-lg);
      padding: var(--space-2);
    }

    #verfuegbarkeit .mobile-helper {
      display: block;
      margin-bottom: var(--space-1);
    }

    #verfuegbarkeit .month-events__list {
      padding: var(--space-2);
    }
  }

  @media (max-width: 520px) {
    #verfuegbarkeit #calendar-weekdays,
    #verfuegbarkeit #calendar-grid {
      grid-template-columns: repeat(7, minmax(2.15rem, 1fr));
      min-width: 320px;
    }

    #verfuegbarkeit .calendar-nav {
      width: 100%;
      justify-content: space-between;
    }

    #verfuegbarkeit .calendar__day {
      min-height: 3.6rem;
    }

    #verfuegbarkeit .month-event {
      grid-template-columns: 1fr;
    }
  }

  #verfuegbarkeit .calendar__day {
    background: #ffffff;
    border-radius: var(--radius-lg);
    border: 1px solid rgba(15, 17, 21, 0.08);
    box-shadow: 0 12px 32px rgba(15, 17, 21, 0.1);
    transition: border-color 180ms ease, box-shadow 180ms ease, transform 180ms ease, background 180ms ease;
    min-height: 5.4rem;
    padding: var(--space-2);
    display: grid;
    gap: var(--space-2);
    align-content: space-between;
    position: relative;
  }

  #verfuegbarkeit .calendar__day--busy {
    border-color: rgba(255, 113, 91, 0.28);
    background: linear-gradient(180deg, #fff8f4, #fff5ee);
  }

  #verfuegbarkeit .calendar__day--free {
    border-color: rgba(15, 17, 21, 0.08);
    background: linear-gradient(180deg, #ffffff, #f8fafc);
  }

  #verfuegbarkeit .calendar__day:hover,
  #verfuegbarkeit .calendar__day:focus-visible {
    border-color: rgba(255, 113, 91, 0.45);
    box-shadow: 0 16px 40px rgba(255, 113, 91, 0.18);
    transform: translateY(-4px);
  }

  #verfuegbarkeit .calendar__day--active {
    border-color: rgba(255, 113, 91, 0.65);
    box-shadow: 0 18px 46px rgba(255, 113, 91, 0.28);
    transform: translateY(-4px);
  }

  #verfuegbarkeit .calendar__day-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-2);
  }

  #verfuegbarkeit .calendar__day-number {
    color: #0f1115;
    font-weight: 800;
    font-size: 1.25rem;
    letter-spacing: 0.01em;
  }

  #verfuegbarkeit .calendar__day-status {
    border-radius: 999px;
    padding: 6px 10px;
    font-size: var(--font-size-xs);
    font-weight: 700;
    letter-spacing: 0.02em;
    text-transform: uppercase;
    border: 1px solid rgba(15, 17, 21, 0.08);
    background: rgba(15, 17, 21, 0.04);
    color: #0f1115;
  }

  #verfuegbarkeit .calendar__day-status--busy {
    background: rgba(255, 113, 91, 0.14);
    border-color: rgba(255, 113, 91, 0.35);
    color: #9c2a17;
  }

  #verfuegbarkeit .calendar__day-status--free {
    background: rgba(16, 185, 129, 0.12);
    border-color: rgba(16, 185, 129, 0.28);
    color: #0b714a;
  }

  #verfuegbarkeit .calendar__event-bar {
    width: 100%;
    height: 10px;
    border-radius: 999px;
    background: rgba(15, 17, 21, 0.06);
    border: 1px solid rgba(15, 17, 21, 0.08);
    box-shadow: inset 0 1px 1px rgba(255, 255, 255, 0.8);
  }

  #verfuegbarkeit .calendar__event-bar--busy {
    background: linear-gradient(135deg, #ff2f2f, #ff715b);
    border-color: rgba(255, 47, 47, 0.6);
    box-shadow: 0 10px 20px rgba(255, 71, 71, 0.25);
  }

  #verfuegbarkeit .calendar__chips {
    display: grid;
    gap: 6px;
    width: 100%;
  }

  #verfuegbarkeit .calendar__chip {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 6px 10px;
    border-radius: 12px;
    background: rgba(15, 17, 21, 0.05);
    color: #0f1115;
    font-size: var(--font-size-sm);
    line-height: 1.3;
    border: 1px solid rgba(15, 17, 21, 0.06);
  }

  #verfuegbarkeit .calendar__chip--count {
    background: rgba(255, 113, 91, 0.12);
    border-color: rgba(255, 113, 91, 0.24);
    color: #9e2d16;
    font-weight: 700;
  }

  #verfuegbarkeit .calendar__markers,
  #verfuegbarkeit .calendar__marker,
  #verfuegbarkeit .calendar__marker--count {
    display: none;
  }

  #verfuegbarkeit .pill-muted {
    background: rgba(15, 17, 21, 0.06);
    border-radius: 999px;
    padding: 6px 12px;
    font-size: var(--font-size-xs);
    color: var(--color-muted);
  }

  #verfuegbarkeit .event-date {
    display: grid;
    gap: 2px;
    text-align: center;
    background: linear-gradient(135deg, #ff715b, #ffb347);
    color: white;
    padding: var(--space-2) var(--space-3);
    border-radius: var(--radius-md);
    min-width: 120px;
  }

  #verfuegbarkeit .event-date .weekday {
    font-size: var(--font-size-sm);
    letter-spacing: 0.04em;
    text-transform: uppercase;
    opacity: 0.9;
  }

  #verfuegbarkeit .event-date .day-number {
    font-size: 1.75rem;
    font-weight: 800;
    line-height: 1;
  }

  #verfuegbarkeit .event-content {
    display: grid;
    gap: 6px;
  }

  #verfuegbarkeit .event-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-1);
  }

  #verfuegbarkeit .event-title {
    margin: 0;
  }

  #verfuegbarkeit .event-meta {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
    color: var(--color-muted);
    font-size: var(--font-size-sm);
  }

  #verfuegbarkeit .meta-chip {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 6px 10px;
    border-radius: 999px;
    background: rgba(15, 17, 21, 0.04);
  }

  #verfuegbarkeit .event-description {
    margin: 0;
    color: var(--color-text);
  }

  #verfuegbarkeit .month-events {
    margin-top: var(--space-3);
    background: linear-gradient(180deg, rgba(15, 17, 21, 0.03), rgba(255, 255, 255, 0.9));
    border: 1px solid rgba(15, 17, 21, 0.05);
    box-shadow: 0 18px 50px rgba(15, 17, 21, 0.12);
  }

  #verfuegbarkeit .month-events__header {
    display: flex;
    justify-content: space-between;
    gap: var(--space-2);
    align-items: flex-start;
    flex-wrap: wrap;
    padding: var(--space-3) var(--space-3) 0;
  }

  #verfuegbarkeit .month-events__list {
    display: grid;
    gap: var(--space-2);
    padding: var(--space-3);
  }

  #verfuegbarkeit .month-event {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: var(--space-2);
    align-items: center;
    border: 1px solid rgba(15, 17, 21, 0.08);
    border-radius: var(--radius-md);
    padding: var(--space-2);
    background: white;
    transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease;
  }

  #verfuegbarkeit .month-event:hover {
    border-color: rgba(255, 113, 91, 0.35);
    box-shadow: 0 12px 26px rgba(15, 17, 21, 0.12);
    transform: translateY(-2px);
  }

  #verfuegbarkeit .month-event--active {
    border-color: rgba(255, 113, 91, 0.5);
    box-shadow: 0 14px 32px rgba(255, 113, 91, 0.18);
    transform: translateY(-2px);
  }

  #verfuegbarkeit .month-event__title {
    margin: 0 0 4px 0;
  }

  #verfuegbarkeit .month-event__meta {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
    color: var(--color-muted);
    font-size: var(--font-size-sm);
  }

  #verfuegbarkeit .calendar__empty {
    margin: 0;
    color: var(--color-muted);
  }

  @media (min-width: 960px) {
    #verfuegbarkeit .calendar__day {
      gap: var(--space-2);
      min-height: 5.2rem;
    }

    #verfuegbarkeit .calendar__chips {
      grid-template-columns: 1fr;
    }
  }
</style>
<script type="module" src={calendarScript} defer></script>
