<section id="verfuegbarkeit" class="section">
  <div class="container">
    <h2>Events</h2>
    <div class="card calendar-card">
      <header class="calendar-toolbar">
        <div class="calendar-nav">
          <button id="calendar-prev" class="btn btn-secondary" aria-label="Vorheriger Monat">‹</button>
          <strong id="calendar-month"></strong>
          <button id="calendar-next" class="btn btn-secondary" aria-label="Nächster Monat">›</button>
        </div>
      </header>
      <div id="calendar-status" class="price-pill">Kalender wird geladen …</div>
      <div
        id="calendar-weekdays"
        class="calendar__weekdays"
        style="display:grid;grid-template-columns:repeat(7,1fr);gap:var(--space-2);margin:var(--space-3) 0;"
      >
        {['Mo', 'Di', 'Mi', 'Do', 'Fr', 'Sa', 'So'].map((d) => (
          <div style="text-align:center;font-weight:600;">{d}</div>
        ))}
      </div>
      <div id="calendar-grid" class="grid" style="grid-template-columns: repeat(7, 1fr);"></div>
      <div id="calendar-events" class="cards" style="margin-top: var(--space-4);"></div>
    </div>
  </div>
</section>
<style>
  #verfuegbarkeit h2 {
    color: var(--color-corporate-white);
  }

  #verfuegbarkeit p {
    color: rgba(255, 255, 255, 0.85);
  }

  #verfuegbarkeit .calendar-card {
    background: linear-gradient(145deg, rgba(255, 255, 255, 0.97), rgba(255, 255, 255, 0.92));
    border: 1px solid rgba(255, 255, 255, 0.2);
    box-shadow: var(--shadow-strong);
  }

  #verfuegbarkeit .calendar-toolbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: var(--space-3);
    flex-wrap: wrap;
    padding: var(--space-2) var(--space-1) var(--space-3);
    border-bottom: 1px solid rgba(15, 17, 21, 0.08);
  }

  #verfuegbarkeit .calendar-nav {
    display: flex;
    gap: var(--space-2);
    align-items: center;
  }

  #verfuegbarkeit .calendar-hint {
    margin: 0;
    font-size: var(--font-size-sm);
    color: var(--color-muted);
  }

  #verfuegbarkeit .btn-secondary {
    background: linear-gradient(135deg, var(--color-primary), #ff947c);
    color: var(--color-corporate-white);
    border: none;
    box-shadow: 0 10px 24px rgba(255, 113, 91, 0.28);
  }

  #verfuegbarkeit .btn-secondary:hover,
  #verfuegbarkeit .btn-secondary:focus-visible {
    filter: brightness(0.98);
    transform: translateY(-1px);
  }

  #verfuegbarkeit #calendar-month {
    font-size: var(--font-size-lg);
    color: var(--color-text);
  }

  #verfuegbarkeit .calendar__weekdays {
    background: rgba(255, 113, 91, 0.06);
    padding: var(--space-2);
    border-radius: var(--radius-md);
  }

  #verfuegbarkeit .calendar__day {
    border: 1px solid rgba(15, 17, 21, 0.08);
    background: var(--color-corporate-white);
    transition: border-color 160ms ease, box-shadow 160ms ease, transform 160ms ease;
  }

  #verfuegbarkeit .calendar__day--active {
    border-color: rgba(255, 113, 91, 0.5);
    box-shadow: 0 12px 28px rgba(255, 113, 91, 0.22);
    transform: translateY(-4px) scale(1.03);
  }

  #verfuegbarkeit .calendar__day-number {
    color: var(--color-text);
  }

  #verfuegbarkeit .calendar__event {
    transition: border-color 160ms ease, box-shadow 160ms ease, transform 160ms ease;
  }

  #verfuegbarkeit .calendar__event--active {
    border-color: rgba(255, 113, 91, 0.5);
    box-shadow: 0 12px 28px rgba(255, 113, 91, 0.22);
    transform: translateY(-4px) scale(1.03);
  }
</style>
<script type="module" src="/src/lib/calendar.ts" defer></script>
