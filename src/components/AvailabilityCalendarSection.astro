<section id="verfuegbarkeit" class="section">
  <div class="container">
    <div class="section-header">
      <p class="eyebrow">Events &amp; Auftritte</p>
      <h2>Erlebe Archie live und in Farbe</h2>
      <p class="subtitle">
        Übersichtliche Planung mit direkter Anbindung an den Kalender. Tippe oder fahre über ein Datum, um die passenden
        Termine hervorzuheben.
      </p>
    </div>
    <div class="card calendar-card">
      <header class="calendar-toolbar">
        <div class="calendar-nav">
          <button id="calendar-prev" class="btn btn-secondary" aria-label="Vorheriger Monat">‹</button>
          <div class="calendar-month">
            <strong id="calendar-month"></strong>
            <span class="calendar-hint">Automatisch synchronisiert</span>
          </div>
          <button id="calendar-next" class="btn btn-secondary" aria-label="Nächster Monat">›</button>
        </div>
        <div class="calendar-legend">
          <span class="legend-dot legend-dot--primary"></span>
          <span>Bestätigte Termine</span>
        </div>
      </header>
      <div id="calendar-status" class="price-pill calendar-status" aria-live="polite">Kalender wird geladen …</div>
      <div class="calendar-layout">
        <div class="calendar-board">
          <div id="calendar-weekdays" class="calendar__weekdays">
            {['Mo', 'Di', 'Mi', 'Do', 'Fr', 'Sa', 'So'].map((d) => (
              <div class="calendar__weekday">{d}</div>
            ))}
          </div>
          <div id="calendar-grid" class="grid"></div>
        </div>
        <aside class="calendar-panel">
          <div class="panel-header">
            <div>
              <p class="eyebrow">Nächste Events</p>
              <h3>Alle Termine im Detail</h3>
            </div>
            <span class="pill-muted">Live-Updates</span>
          </div>
          <div id="calendar-events" class="cards calendar__event-list"></div>
        </aside>
      </div>
    </div>
  </div>
</section>
<style>
  #verfuegbarkeit h2 {
    color: var(--color-corporate-white);
  }

  #verfuegbarkeit .section-header {
    display: grid;
    gap: var(--space-1);
    margin-bottom: var(--space-4);
    max-width: 760px;
  }

  #verfuegbarkeit .subtitle {
    color: var(--color-muted);
    margin: 0;
  }

  #verfuegbarkeit .eyebrow {
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-size: var(--font-size-xs);
    color: var(--color-muted);
    margin: 0;
  }

  #verfuegbarkeit .calendar-card {
    background: radial-gradient(circle at 30% 20%, rgba(255, 113, 91, 0.08), transparent 45%),
      radial-gradient(circle at 70% 40%, rgba(74, 200, 255, 0.1), transparent 42%),
      linear-gradient(145deg, rgba(255, 255, 255, 0.98), rgba(255, 255, 255, 0.9));
    border: 1px solid rgba(255, 255, 255, 0.2);
    box-shadow: 0 12px 42px rgba(15, 17, 21, 0.12);
    overflow: hidden;
  }

  #verfuegbarkeit .calendar-toolbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: var(--space-4);
    flex-wrap: wrap;
    padding: var(--space-2) var(--space-1) var(--space-3);
    border-bottom: 1px solid rgba(15, 17, 21, 0.08);
  }

  #verfuegbarkeit .calendar-nav {
    display: flex;
    gap: var(--space-3);
    align-items: center;
  }

  #verfuegbarkeit .calendar-month {
    display: grid;
    gap: 2px;
  }

  #verfuegbarkeit .calendar-hint {
    margin: 0;
    font-size: var(--font-size-xs);
    color: var(--color-muted);
  }

  #verfuegbarkeit .btn-secondary {
    background: rgba(15, 17, 21, 0.06);
    color: var(--color-text);
    border: 1px solid rgba(15, 17, 21, 0.12);
    box-shadow: 0 10px 30px rgba(15, 17, 21, 0.14);
    padding: var(--space-2);
    border-radius: 999px;
    width: 2.75rem;
    height: 2.75rem;
  }

  #verfuegbarkeit .btn-secondary:hover,
  #verfuegbarkeit .btn-secondary:focus-visible {
    background: rgba(15, 17, 21, 0.08);
    box-shadow: 0 12px 28px rgba(15, 17, 21, 0.16);
  }

  #verfuegbarkeit #calendar-month {
    font-size: var(--font-size-xl);
    color: var(--color-text);
  }

  #verfuegbarkeit .calendar-legend {
    display: inline-flex;
    align-items: center;
    gap: var(--space-1);
    font-size: var(--font-size-sm);
    color: var(--color-muted);
  }

  #verfuegbarkeit .legend-dot {
    width: 10px;
    height: 10px;
    border-radius: 999px;
    display: inline-block;
  }

  #verfuegbarkeit .legend-dot--primary {
    background: linear-gradient(135deg, #ff715b, #ffb347);
    box-shadow: 0 0 0 4px rgba(255, 113, 91, 0.18);
  }

  #verfuegbarkeit .calendar-status {
    margin: 0 var(--space-1) var(--space-2);
  }

  #verfuegbarkeit .calendar__weekdays {
    background: rgba(255, 113, 91, 0.08);
    padding: var(--space-2);
    border-radius: var(--radius-md);
  }

  #verfuegbarkeit .calendar-layout {
    width: 100%;
    display: grid;
    gap: var(--space-4);
    grid-template-columns: minmax(360px, 1.15fr) minmax(320px, 0.9fr);
    align-items: start;
    padding: 0 var(--space-1) var(--space-3);
    box-sizing: border-box;
  }

  #verfuegbarkeit .calendar-board {
    width: 100%;
    overflow-x: auto;
    padding-bottom: var(--space-1);
  }

  #verfuegbarkeit #calendar-weekdays,
  #verfuegbarkeit #calendar-grid {
    display: grid;
    grid-template-columns: repeat(7, minmax(3rem, 1fr));
    gap: var(--space-2);
    width: 100%;
    min-width: 420px;
  }

  #verfuegbarkeit #calendar-weekdays {
    margin: var(--space-3) 0;
  }

  #verfuegbarkeit .calendar__weekday {
    text-align: center;
    font-weight: 600;
  }

  @media (max-width: 720px) {
    #verfuegbarkeit #calendar-weekdays,
    #verfuegbarkeit #calendar-grid {
      grid-template-columns: repeat(7, minmax(2.5rem, 1fr));
      gap: var(--space-1);
      min-width: 380px;
    }

    #verfuegbarkeit .calendar-layout {
      grid-template-columns: 1fr;
    }

    #verfuegbarkeit .calendar-toolbar {
      flex-direction: column;
      align-items: flex-start;
    }

    #verfuegbarkeit #calendar-weekdays {
      padding: var(--space-2) var(--space-1);
      margin: var(--space-2) 0;
    }

    #verfuegbarkeit .calendar__weekday {
      font-size: var(--font-size-sm);
    }
  }

  @media (max-width: 520px) {
    #verfuegbarkeit #calendar-weekdays,
    #verfuegbarkeit #calendar-grid {
      grid-template-columns: repeat(7, minmax(2.25rem, 1fr));
      min-width: 340px;
    }
  }

  #verfuegbarkeit .calendar__day {
    border: 1px solid rgba(15, 17, 21, 0.08);
    background: var(--color-corporate-white);
    border-radius: var(--radius-md);
    transition: border-color 160ms ease, box-shadow 160ms ease, transform 160ms ease, background 160ms ease;
    min-height: 5.5rem;
    position: relative;
    overflow: hidden;
  }

  #verfuegbarkeit .calendar__day::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, rgba(255, 113, 91, 0.08), transparent 60%);
    opacity: 0;
    transition: opacity 160ms ease;
  }

  #verfuegbarkeit .calendar__day--active {
    border-color: rgba(255, 113, 91, 0.5);
    box-shadow: 0 12px 28px rgba(255, 113, 91, 0.22);
    transform: translateY(-4px) scale(1.03);
    background: #fff7f4;
  }

  #verfuegbarkeit .calendar__day--active::after {
    opacity: 1;
  }

  #verfuegbarkeit .calendar__day-number {
    color: var(--color-text);
    font-weight: 700;
  }

  #verfuegbarkeit .calendar__chips {
    display: grid;
    gap: 6px;
    margin-top: var(--space-2);
  }

  #verfuegbarkeit .calendar__badge {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    background: rgba(255, 113, 91, 0.12);
    color: var(--color-text);
    padding: 6px 10px;
    border-radius: 10px;
    border: 1px solid rgba(255, 113, 91, 0.2);
    font-size: var(--font-size-sm);
    line-height: 1.2;
  }

  #verfuegbarkeit .calendar__badge--muted {
    background: rgba(15, 17, 21, 0.05);
    border-color: rgba(15, 17, 21, 0.08);
  }

  #verfuegbarkeit .calendar__event {
    transition: border-color 160ms ease, box-shadow 160ms ease, transform 160ms ease;
  }

  #verfuegbarkeit .calendar__event--active {
    border-color: rgba(255, 113, 91, 0.5);
    box-shadow: 0 12px 28px rgba(255, 113, 91, 0.22);
    transform: translateY(-4px) scale(1.03);
  }

  #verfuegbarkeit .calendar-panel {
    background: rgba(15, 17, 21, 0.02);
    border: 1px solid rgba(15, 17, 21, 0.05);
    border-radius: var(--radius-lg);
    padding: var(--space-3);
    display: grid;
    gap: var(--space-3);
  }

  #verfuegbarkeit .panel-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-2);
    flex-wrap: wrap;
  }

  #verfuegbarkeit .pill-muted {
    background: rgba(15, 17, 21, 0.06);
    border-radius: 999px;
    padding: 4px 10px;
    font-size: var(--font-size-xs);
    color: var(--color-muted);
  }

  #verfuegbarkeit .calendar__event-list {
    display: grid;
    gap: var(--space-2);
  }

  #verfuegbarkeit .calendar__event {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: var(--space-2);
    align-items: center;
    border: 1px solid rgba(15, 17, 21, 0.08);
  }

  #verfuegbarkeit .event-date {
    display: grid;
    gap: 2px;
    text-align: center;
    background: linear-gradient(135deg, #ff715b, #ffb347);
    color: white;
    padding: var(--space-2) var(--space-3);
    border-radius: var(--radius-md);
    min-width: 120px;
  }

  #verfuegbarkeit .event-date .weekday {
    font-size: var(--font-size-sm);
    letter-spacing: 0.04em;
    text-transform: uppercase;
    opacity: 0.9;
  }

  #verfuegbarkeit .event-date .day-number {
    font-size: 1.75rem;
    font-weight: 800;
    line-height: 1;
  }

  #verfuegbarkeit .event-content {
    display: grid;
    gap: 6px;
  }

  #verfuegbarkeit .event-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-1);
  }

  #verfuegbarkeit .event-title {
    margin: 0;
  }

  #verfuegbarkeit .event-meta {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
    color: var(--color-muted);
    font-size: var(--font-size-sm);
  }

  #verfuegbarkeit .meta-chip {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 6px 10px;
    border-radius: 999px;
    background: rgba(15, 17, 21, 0.04);
  }

  #verfuegbarkeit .event-description {
    margin: 0;
    color: var(--color-text);
  }
</style>
<script type="module" src="/src/lib/calendar.ts" defer></script>
