<section id="anfrage" class="section">
  <div class="container">
    <h2>Anfrage</h2>
    <p>Schicken Sie uns die wichtigsten Eckdaten. Wir melden uns schnellstmöglich.</p>
    <form id="inquiry-form" class="card form">
      <div class="form__grid">
        {[
          { id: 'company', label: 'Firma', type: 'text', required: true },
          { id: 'contact', label: 'Ansprechpartner*in', type: 'text', required: true },
          { id: 'email', label: 'E-Mail', type: 'email', required: true },
          { id: 'phone', label: 'Telefon', type: 'tel', required: true },
          { id: 'date', label: 'Datum', type: 'date', required: true },
          { id: 'city', label: 'Ort', type: 'text', required: true },
          { id: 'people', label: 'Personenanzahl', type: 'number', required: true },
          { id: 'event', label: 'Art der Veranstaltung', type: 'text', required: true }
        ].map((field) => (
          <div class="form__field">
            <label for={field.id}>{field.label}</label>
            <input id={field.id} name={field.id} type={field.type} required={field.required} />
            <div class="error" id={`${field.id}-error`}></div>
          </div>
        ))}
      </div>
      <div class="form__field">
        <label for="message">Nachricht</label>
        <textarea id="message" name="message" rows="4"></textarea>
        <div class="error" id="message-error"></div>
      </div>
      <div id="inquiry-success" class="form__success is-hidden">
        Vielen Dank für Ihre Anfrage! Bitte füllen Sie nun den Vertrag aus.
      </div>
      <div class="form__actions">
        <button type="submit" class="btn btn-primary">Absenden</button>
        <button type="reset" class="btn btn-secondary">Zurücksetzen</button>
      </div>
    </form>
  </div>
</section>
<script type="module" src="/src/lib/forms.ts" defer></script>
