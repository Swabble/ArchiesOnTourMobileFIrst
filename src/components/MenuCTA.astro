<section id="menu-cta" class="section menu-cta-section">
  <!-- Burger image that slides down from top as panel scrolls away -->
  <div class="burger-parallax">
    <img src="/assets/Burger.jpeg" alt="Archies Burger" class="burger-image" />
  </div>
  <div class="container">
    <div class="menu-cta">
      <div class="menu-cta__content">
        <h2>Unsere Produktauswahl</h2>
        <p class="menu-cta__text">
          Entdecke unser vielfältiges Angebot an frischen Burgern, Beilagen und Getränken – perfekt für jedes Event!
        </p>
      </div>
      <div class="menu-cta__action">
        <a href="/menu" class="btn btn-primary">
          Zum Menü
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .menu-cta-section {
    position: relative;
    /* Background removed - using default section background */
  }

  .burger-parallax {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%) translateY(-200vh);
    width: clamp(300px, 50vw, 600px);
    height: auto;
    z-index: 5;
    pointer-events: none;
    will-change: transform;
  }

  .burger-image {
    width: 100%;
    height: auto;
    display: block;
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-soft);
  }

  .menu-cta {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    align-items: center;
    text-align: center;
    padding: clamp(var(--space-5), 6vw, var(--space-8));
  }

  .menu-cta__content {
    max-width: 600px;
  }

  .menu-cta__content h2 {
    margin: 0 0 var(--space-3);
    font-size: var(--font-size-3xl);
    color: var(--color-corporate-white);
  }

  .menu-cta__text {
    margin: 0;
    font-size: var(--font-size-lg);
    color: rgba(255, 255, 255, 0.9);
    line-height: 1.6;
  }

  .menu-cta__action {
    display: flex;
    gap: var(--space-3);
    flex-wrap: wrap;
    justify-content: center;
  }

  @media (min-width: 768px) {
    .menu-cta {
      flex-direction: row;
      justify-content: space-between;
      text-align: left;
    }

    .menu-cta__action {
      justify-content: flex-end;
    }
  }
</style>
