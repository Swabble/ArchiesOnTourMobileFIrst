<section id="menu" class="section menu-section" aria-live="polite">
  <div class="container">
    <header class="section-header">
      <p class="eyebrow">Unsere Produktauswahl</p>
      <h2>Frisch, flexibel & bereit für euer Event</h2>
      <p class="section-lead">
        Wir laden die Menü-Daten aus der API, zeigen dir die Rohantwort und rendern die Artikel nach Kategorie. So siehst du
        jederzeit, was wirklich aus dem Backend kommt.
      </p>
    </header>

    <div class="menu-hero">
      <div class="menu-hero__status" aria-live="polite">
        <p id="menu-loading" class="status-pill">Menü wird geladen …</p>
        <p id="menu-error" class="status-pill status-pill--error is-hidden">Fehler beim Laden der Daten. Fallback aktiv.</p>
        <p id="menu-source" class="status-pill status-pill--muted is-hidden"></p>
      </div>

      <section class="card menu-hero__stats" id="menu-stats" aria-live="polite">
        <header class="menu-panel__header">
          <p class="eyebrow">Überblick</p>
          <h3>Menü-Status & Statistiken</h3>
          <p>Quelle, Anzahl der Elemente und Kategorien werden nach dem Laden automatisch aktualisiert.</p>
        </header>
        <div class="menu-stats__grid">
          <div class="menu-stats__item">
            <p class="menu-stats__label">Produkte</p>
            <p id="menu-stat-count" class="menu-stats__value">–</p>
          </div>
          <div class="menu-stats__item">
            <p class="menu-stats__label">Kategorien</p>
            <p id="menu-stat-categories" class="menu-stats__value">–</p>
          </div>
          <div class="menu-stats__item">
            <p class="menu-stats__label">Quelle</p>
            <p id="menu-stat-source" class="menu-stats__value">–</p>
          </div>
        </div>
      </section>
    </div>

    <div class="menu-content">
      <section class="card menu-panel" id="menu-json-card" aria-live="polite">
        <header class="menu-panel__header">
          <p class="eyebrow">API-Rohdaten</p>
          <h3>JSON-Antwort</h3>
          <p id="menu-json-meta" class="menu-panel__meta">Wird geladen …</p>
        </header>
        <pre id="menu-json-raw" class="menu-json__pre">Noch keine Daten geladen.</pre>
      </section>

      <section class="card menu-panel" aria-live="polite">
        <header class="menu-panel__header">
          <p class="eyebrow">Gerenderte Daten</p>
          <h3>Produkte aus der API</h3>
          <p>Alle Artikel werden nach Kategorie gruppiert. Jede Karte zeigt Titel, Preis, Beschreibung und optionale Hinweise.</p>
        </header>
        <div id="menu-categories-container" class="menu-categories" aria-live="polite">
          <p class="menu-empty">Noch keine Produkte geladen.</p>
        </div>
      </section>
    </div>
  </div>
</section>
<script type="module">
  import { bootstrapMenu } from '../lib/fetchMenu.ts';

  bootstrapMenu();
</script>
