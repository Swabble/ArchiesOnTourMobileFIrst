<section id="angebot" class="section">
  <div class="container">
    <h2>Unser Angebot</h2>
    <p>Archie on Tour bringt Streetfood-Feeling auf Firmenfeiern, Festivals und private Events.</p>
    <div class="cards">
      {[
        {
          icon: '🍽️',
          title: 'Firmenfeiern',
          features: [
            'Bis zu 500 Personen',
            'Region: Großraum & Umland',
            'Live-Cooking für Gäste',
            'Optionen für vegetarisch/vegan'
          ]
        },
        {
          icon: '🎪',
          title: 'Messen & Gewerbeschauen',
          features: [
            'Schnelle Ausgabezeiten',
            'Branding des Trucks möglich',
            'Mehrsprachiges Service-Team',
            'Perfekt für B2B-Networking'
          ]
        },
        {
          icon: '🎉',
          title: 'Festivals & Straßenfeste',
          features: [
            'Hohe Durchlaufkapazität',
            'Mehrere Standorte gleichzeitig',
            'Flexibles Menüangebot',
            'Erfahrung mit Großveranstaltungen'
          ]
        },
        {
          icon: '💒',
          title: 'Hochzeiten',
          features: [
            'Elegante Präsentation',
            'Individuelle Menügestaltung',
            'Abstimmung auf Hochzeitsthema',
            'Service für Abend- oder Mitternachtssnack'
          ]
        },
        {
          icon: '🏆',
          title: 'Vereinsfeste & Stadtfeste',
          features: [
            'Familien- und kinderfreundliche Optionen',
            'Attraktive Preisgestaltung',
            'Lokale Verankerung',
            'Flexible Betriebszeiten'
          ]
        },
        {
          icon: '🤝',
          title: 'Team-Events',
          features: [
            'Gemeinsames Kocherlebnis',
            'Workshops & Tastings',
            'Anpassung an Allergien',
            'Indoor & Outdoor möglich'
          ]
        },
        {
          icon: '🏡',
          title: 'Private Events',
          features: [
            'Geburtstage & Jubiläen',
            'Individuelle Menüs ab 40 Personen',
            'Regionale Zutaten',
            'Komplettes Service-Team'
          ]
        }
      ].map((item) => (
        <article class="card offer-card">
          <div class="offer-card__header">
            <div class="offer-card__icon">{item.icon}</div>
            <h3 class="offer-card__title">{item.title}</h3>
            <button class="offer-card__toggle" aria-label="Details anzeigen">
              <svg class="offer-card__arrow" width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M5 7.5L10 12.5L15 7.5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </button>
          </div>
          <ul class="offer-card__features">
            {item.features.map((feature) => (
              <li>{feature}</li>
            ))}
          </ul>
        </article>
      ))}
    </div>
  </div>
</section>

<script is:inline>
  // Toggle offer card expansion on mobile
  (function() {
    function initOfferCards() {
      const cards = document.querySelectorAll('.offer-card');

      cards.forEach((card) => {
        const toggle = card.querySelector('.offer-card__toggle');

        if (toggle) {
          toggle.addEventListener('click', (e) => {
            e.preventDefault();
            card.classList.toggle('is-expanded');

            const isExpanded = card.classList.contains('is-expanded');
            toggle.setAttribute('aria-label', isExpanded ? 'Details ausblenden' : 'Details anzeigen');
          });
        }
      });
    }

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initOfferCards);
    } else {
      initOfferCards();
    }
  })();
</script>
